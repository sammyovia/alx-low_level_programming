#include "main.h"
/**
 * print_alphabet - prints the alphbets in lowrrcase
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
void print_alphabet(void)
{
	int g;

	for (g = 'a'; g <= 'z';	g++)
	{
		_putchar(g);

	}
	_putchar('\n');
}
