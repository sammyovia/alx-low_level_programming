#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 * Description:prints the alphabets in lower case 
 *
 */

int main(void)
{
	int ch;
	for(ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);	
	return (0);
}
